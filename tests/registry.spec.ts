import { expect } from 'chai';
import * as path from 'path';

describe('registry', function () {
	describe('registry test1', function () {
		it('should have ', function () {
			expect('test1').to.deep.equal([]);
		});
    });
});